
const word = "My life is amazing";
const sub = 'ama';
const wordArr = [...word];

for(let i=0; i < wordArr.length;i++)
{
    if(wordArr[i] != '')
    {


        // if(i === 0)
        // {

        // }else if(wordArr[i+1] === ''){

        // }

    }
}